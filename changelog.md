# 更新日志 / Changelog

本文件记录 素构 Plainstruct 的所有重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.3.0] - 2026-08-31

### 新增

- 文档手动排序：内容树不再强制按任何规则排列，直接把文档或文件夹拖到目标行的上/下边缘即可按自己的意愿排列（同目录重排，跨目录移动后插入到目标位置），多选拖动整体成组插入；顺序保存在站点 `.plainstruct/order.json` 中，新建与导入的文档不打乱既有排列
- 构建站点完全跟随手动排序：导航、目录页、上/下篇与自动目录列表页均按内容树中的排列顺序输出
- Logo 失焦入睡动画：窗口未聚焦时 Logo 先转头来回看一看，随后闭眼睡觉（Zzz 字符错峰上飘循环），整体以透明度变淡（深浅主题通用）；窗口重新聚焦后透明度缓缓恢复、Zzz 渐隐、Logo 缓缓睁眼，继续原有的眨眼、鼠标跟踪与点按反馈
- 站点设置「站点语言」支持自定义：除预设外可选择「自定义」并输入任意语言代号（如 ja-JP），写入生成页面的 `<html lang>`
- 设置页按类别分页：语言、编辑器、个性化、关于四类设置一次只显示一类，点击左侧导航切换；页面按导航方向整体滑入滑出，页内设置行次第浮现，全部使用非线性缓动
- macOS 适配：红绿灯常驻左上角并采用系统配色（悬停显示控制符号）；品牌 Logo 与站点路径显示移至右上角，Logo 固定在最右、路径显示在其左侧，「返回主菜单」悬停动画方向随布局镜像
- GitHub Actions 新增 macOS arm64（Apple Silicon）构建：自动产出 dmg 磁盘镜像并随标签附加到 Release 草稿

### 修复

- 修复在设置页点击标题栏「返回主菜单」时站点被静默关闭、界面却停留在设置页的问题：设置页不依赖站点，该入口在设置页暂时禁用（不显示悬停提示、点击无动作）
- Logo 唤醒时序打磨：窗口重新聚焦后先恢复透明度、Zzz 渐渐消失，随后 Logo 才缓缓睁眼并继续眨眼循环、鼠标跟踪与点按反馈；唤醒途中再度失焦会直接睡下

### 变更

- macOS 图标适配 macOS 26+（Tahoe）规范：`icon.icns` 改为满铺（full-bleed）1024×1024 内容，圆角与形状交给系统统一裁切与 Liquid Glass 效果，不再使用 Big Sur 时代自带留白的画布；`scripts/gen-mac-icon.py` 同步更新
- macOS 窗口改用原生装饰：原生圆角与原生红绿灯控件（`titleBarStyle: Overlay` + 隐藏原生标题，启用 `macos-private-api`），标题栏内容延伸至窗口顶部；Windows/Linux 仍为无边框自绘标题栏
- macOS 标题栏 Logo 位于右上角时，眼睛水平注视幅度减半（新增 `gazeScaleX` 参数），修正因 logo 贴近窗口右缘导致的视线过度偏左
- GitHub Actions macOS 构建对 .app 与 dmg 做 ad-hoc 签名，降低无公证应用被 Gatekeeper 判定为「已损坏」的概率；Release 说明改为提供 `xattr -cr /Applications/Plainstruct.app` 解除隔离命令（未公证应用无法完全绕过 Gatekeeper，此为无证书下的最优缓解）
- 关于页介绍文案更新，移除特性列表（保留技术架构与工作室版权署名）
- 版本号升级至 1.3.0，内置主题版本同步升级

## [1.2.0] - 2026-08-30

### 新增

- 个性化设置：设置页新增「个性化」分区，支持软件主题（浅色/暗色/跟随系统，暗色为全新暖黑配色）、界面字体与编辑器字体（系统默认/衬线/等宽/自定义 font-family），即改即生效并持久保存；跟随系统时自动响应系统深浅色切换
- 标题栏 Logo 动画：眼睛隔几秒随机眨一次、小幅度跟随鼠标，鼠标点按任意处时 Logo 轻压回弹并眯眼反馈
- 设置页重构为桌面应用式布局：左侧分区导航与右侧内容滚动联动，设置项改为行式排版
- 自定义下拉选择组件：面板以带轻微过冲的非线性缓动弹出，选项高亮无系统黑色描边，支持键盘操作
- 站点设置新增「站点语言」：写入生成页面的 `<html lang>` 标记，便于搜索引擎与朗读工具识别
- 站点设置新增「浏览器标题格式」：支持 `{page}`/`{site}` 占位符自定义连接符（默认 `{page} · {site}`），输入框下方附格式提示
- 站点 Logo 同步作为全站页面的收藏夹图标（favicon）
- 内置主题侧栏目录支持折叠：文件夹左侧新增小箭头，默认收起；点击展开并跨页记忆，查看文件夹时自动展开且不自动收回，仅用户主动收起时折叠
- 内置主题移动端顶栏在菜单按钮旁显示当前页路径（面包屑），过长时逐级省略
- 内置主题页面过渡动画新增 5 个预设：滑入、弹性浮现、模糊聚焦、翻页、揭开（共 9 个可选）
- 构建页「独立窗口」「打开输出目录」移至构建报告下方，升级为与「重新构建」同规格的按钮

### 修复

- 修复暗色模式下多处配色问题：主菜单/关于页/启动画面的带字 Logo 按主题切换为暖白变体；「新建站点」等强调色实底按钮的文字改用随主题的对比色（新增 `--color-on-accent`/`--color-danger-strong` 令牌）；主题制作器代码高亮、发布页复选框、错误提示、模态遮罩、文本选区与滚动条等改为随主题变量
- 修复标题栏 Logo 眨眼动画偶发错误闪烁：改为定时调度的一次性动画，避免中途修改动画时长导致相位跳变
- 修复主菜单（未打开站点）时标题栏品牌区仍响应悬停动画的问题，悬停「返回主菜单」提示仅在站点打开时出现
- 修复浏览器演示环境下自定义主题创建后列表不刷新的问题

### 变更

- 文档顺序固定：内容树按「目录优先、同级按名称自然排序」排列，不再依赖文件系统返回顺序，构建站点的导航、目录页与上/下篇顺序随之固定
- 关于页介绍文案精简
- 应用图标按 Logo 真实几何重新生成全套尺寸（32px～1024px、.ico/.icns 及移动端图标），解决构建后图标不清晰的问题
- 免安装版压缩包不再附带 logo.svg
- 版本号升级至 1.2.0，内置主题版本同步升级

## [1.1.0] - 2026-08-29

### 新增

- 文件树拖拽改进：拖动文档时显示插入位置横线指示，行上下边缘表示移动到该行所在目录，文件夹中部高亮表示移入该文件夹，树空白处显示根目录末尾插入线
- 内置主题(浅色/暗色)新增「在底部显示由 Plainstruct 创建」开关，默认开启
- 内置主题移动端优化：文档列表自动折叠，点按左上角按钮从左侧弹出文档列表面板，支持遮罩点击、Esc 键与跳转链接时自动关闭
- 文件夹支持生成独立页面：没有 index.md 的文件夹在构建时自动生成目录列表页（`目录/index.html`），导航与目录页中的文件夹标题均可点击跳转
- 新增软件专属右键菜单：禁用原生 WebView 右键菜单；编辑器与输入框内提供剪切/复制/粘贴/全选，文件树内提供新建文档/新建文件夹/导入/重命名/删除，普通文本选区提供复制
- 独立预览窗口记忆位置与尺寸：构建刷新改为原地重载、不再重置窗口位置，重开窗口时恢复上次布局
- 内置主题移动端新增顶部栏：文档列表按钮移入栏内不再遮挡内容，栏右侧显示站点 Logo、名称与描述
- 内置主题站点 Logo 增加悬停反馈（背景高亮、Logo 缩放、强调色），提示可点击返回首页
- 标题栏左上角品牌区悬停时以非线性缓动动画显示「←返回主菜单」

### 修复

- 修复内容页面中将文件拖动到空白处时文档被错误重命名追加 `-2` 后缀的问题(同目录内移动现在为无操作)，重复拖动不再叠加 `-2`
- 修复拖动结束后文件树节点保持半透明状态的问题
- 修复主题配置面板中开关类型字段的标签重复显示

### 变更

- 内置主题上/下篇切换按钮与底部署名样式打磨：悬停浮起、方向箭头位移、署名改为居中胶囊样式
- 引入 tauri-plugin-clipboard-manager 插件支撑右键菜单文本操作
- 版本号升级至 1.1.0，内置主题版本同步升级

## [1.0.0] - 2026-08-29

### 新增

- 编辑器新增 Markdown 格式工具栏，支持加粗、斜体、删除线、标题、引用、列表、链接、表格与代码块操作，附带快捷键与列表自动续行
- 后端新增 `check_update` 命令，对比 GitHub 最新 Release 判断是否有新版本
- 设置页新增应用更新检查入口，展示最新版本、发布说明与发布时间
- 新增引用、列表、链接、表格、代码块等图标，并微调主题样式

## [0.7.0] - 2026-08-29

### 新增

- 新建文档支持自定义标题
- 预览支持关闭与枚举 webview 能力

### 修复

- 构建资源复制改为相对 build 目录落盘，修复页面资源引用

### 变更

- 移除站点 logo 时同步清理配置引用，替换时删除旧文件
- 文件树与界面细节优化

## [0.6.0] - 2026-08-29

### 新增

- 文件树多选模式重构：新增多选模式开关，支持锚点式 Shift 范围选择与 Ctrl/⌘ 切换选中
- 节点点击改为统一的 selectClick 事件并传入 selectMode 状态
- 新增 square/checkSquare 图标及多选相关中英文文案

### 变更

- 关闭窗口原生拖放 (`dragDropEnabled: false`) 以启用网页内拖拽

## [0.5.0] - 2026-08-28

### 新增

- 文件树拖拽支持多选集合整体移动，并以提示反馈结果
- 拖拽悬停折叠文件夹稍候自动展开，便于拖入更深层级

### 变更

- 更新设置图标与设置页渲染逻辑
- 构建器及 plain-dark/plain-light 主题样式更新

## [0.4.0] - 2026-08-25

### 新增

- 支持部分更新站点配置
- 新增编辑首页

### 变更

- 内容树保持原始顺序

## [0.3.0] - 2026-08-25

### 新增

- 文件树支持多选与批量移动

### 变更

- 优化设置图标，调整标题栏布局

## [0.2.0] - 2026-08-25

### 变更

- 升级版本号并更新应用图标

## [0.1.0] - 2026-08-25

### 新增

- 本地静态文档（wiki）网站创建器，基于 Vue 3 + TypeScript + Vite + Tauri 2
- 内容管理：文件树、Markdown 编辑与实时预览
- 一键构建纯静态站点，相对链接不随部署路径变化
- 内置浅色/暗色主题，ZIP 导入导出
- 免 Git 直接发布到 GitHub Pages
- 中英双语界面、自定义标题栏
- 新增关于页与品牌 Logo（graphic + full）
- GitHub Actions 工作流：推送/打标签自动构建并产出安装包

### 修复

- 修复窗口控制（最小化/最大化/关闭）无响应：补全 `core:window` 权限
- 修复独立预览窗口：补全 `core:webview` 权限
